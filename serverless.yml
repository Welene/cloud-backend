service: cloud-backend

provider:
    name: aws
    runtime: nodejs18.x
    region: eu-north-1

functions:
    hello:
        handler: handler.hello
        events:
            - http:
                  path: hello
                  method: get
